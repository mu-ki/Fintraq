@{
    ViewData["Title"] = "Restarting";
}
<section class="page-header">
    <div>
        <p class="eyebrow">Admin</p>
        <h1>Application restart</h1>
        <p class="subtitle mb-0">The application is stopping.</p>
    </div>
</section>
<div class="panel">
    <p class="mb-2">The application has been asked to stop. If you run it under a process manager (e.g. systemd, Docker with restart policy, or a script that restarts on exit), it will start again automatically.</p>
    <p class="mb-0">Otherwise, start the application manually (e.g. run <code>dotnet run</code> or your <code>build-run.bat</code> again).</p>
</div>
